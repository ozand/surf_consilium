[project]
name = "surf-consilium"
version = "0.1.0"
description = "Bounded multi-model advisory council layered on surf-cli"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
surf-consilium = "surf_consilium.council:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
